{
  "name": "ortak-butce",
  "version": "0.1.0",
  "private": true,
  "main": "public/main.js",
  "dependencies": {
    "web-vitals":"^2.1.4"
    "recharts": "^2.12.0"
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "set CI=false&&react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "electron": "electron .",
    "vercel-build": "react-scripts build || exit 0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
